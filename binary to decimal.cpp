#include<stdio.h>
#include<string.h>
#include<math.h>
int main()
{
    char bin[32]={0};
    int dec,i;
    int cnt;
    printf("enter binary value:");
    gets(bin);
    cnt=0;
    dec=0;
    for(i=(strlen(bin)-1;i>=0;i--);
    {
        dec=dec+(bin[i])
    }
}